/**
 * Finish the printAll function
 * @param {Object} a - Object { 1: "value1", 2: "value2" }
 * @returns null "logs the values in the Object"
 */

function printAll(a) {
  // finish the code
}

/**
 * Finish the countLetters function
 * @param {String} a - String
 * @returns "hello -> {h: 1, e: 1, l: 2, o: 1}"
 */

function countLetters(a) {
  // finish the code
}

/**
 *
 * @param {Object} a - Object { 1: "value1", 2: "value2" }
 * @param {String} b - String to find in Object
 * @returns "{ 1: "value1", 2: "value2" }, "value1" -> 1 returns key
 */

function findInObject(a, b) {
  // finish the code
}

/**
 * Finish the storeUserInformation function
 * @param {Object} a - Object { bob: 22, daniel: 23 }
 * @param {Object} b - Object { name: "robert", age: 44 }
 * @returns "{ bob: 22, daniel: 23 }, { name: "robert", age: 44 } -> { bob: 22, daniel: 23, robert: 44 }"
 */

function storeUserInformation(a, b) {
  // finish the code
}

/**
 * Finish the changeUserInformation function
 * @param {Object} a - Object { bob: 22, daniel: 23 }
 * @param {Object} b - Object { name: "bob", age: 44 }
 * @returns "{ bob: 22, daniel: 23 }, { name: "bob", age: 44 } -> { bob: 44, daniel: 23}"
 */

function changeUserInformation(a, b) {
  // finish the code
}

module.exports = {
  printAll,
  countLetters,
  findInObject,
  storeUserInformation,
  changeUserInformation,
};
